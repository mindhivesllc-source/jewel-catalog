# Auto-Sync Implementation Plan (executed inline 2026-09-11)

**Spec:** `docs/superpowers/specs/2026-09-11-catalog-enhancements-design.md` §4

- [x] Schema: `ShopSettings.autoSyncEnabled/syncLockedUntil/lastSyncAt/lastSyncMessage`, `SupplierProduct.lastPushedHash/delistedAt`, `PushJob.trigger` — migration `*_auto_sync`.
- [x] `app/services/sync.server.ts`: `shouldSkipTick`, `planSync` (pure, tested in `sync.server.test.ts`), `runAutoSync`, `runAutoSyncTick`.
- [x] `app/scheduler.server.ts`: 60s interval, started from `entry.server.tsx`; `DISABLE_AUTO_SYNC=1` env kills it.
- [x] Push loop stores `lastPushedHash`; `startPushJob(shop, admin, trigger)`.
- [x] `fetchAndStoreCatalog` returns `stockNos` for delisted detection; partial fetch postpones sync.
- [x] Settings: Auto-sync checkbox + last/next sync; History: Trigger column; Catalog: DELISTED badge.
- [ ] Verify in production: enable toggle, wait ≤16 min, History shows an Auto-sync job or Settings shows "nothing changed".
